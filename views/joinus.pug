extends layout

block content
    include components/navbar
    
    .container
        .jumbotron
            h1 新規登録
            form(action="" method="post")
                .form-group
                    label(for="inputId") ID
                    input.form-control.input-lg#inputId(type="text" name="id" value=inputId placeholder="半角英数字")
                .form-group
                    label(for="inputPassword") パスワード
                    input.form-control.input-lg#inputPassword(type="password" name="password" placeholder="半角英数字のパスワード")
                .form-group
                    label(for="inputPasswordConfirm") パスワードの確認
                    input.form-control.input-lg#inputPasswordConfirm(type="password" name="passwordConfirm" placeholder="確認のためもう一度入力")
                .checkbox
                    label
                        input(type="checkbox" name="agreement" checked)
                        | 私は実験の関係者です

                button.btn.btn-primary.btn-lg.btn-block(type="submit") 登録
        a.btn.btn-link.btn-block(href=`/login?id=${id || ''}`) すでにアカウントをお持ちですか？